release:
  github:
    owner: petermbenjamin
    name: aws-console

build:
  goos:
  - linux
  - darwin
  - windows
  goarch:
  - amd64
  main: cmd/aws-console/main.go
  binary: aws-console

archive:
  format: zip
  files:
  - LICENSE*
  - README*
